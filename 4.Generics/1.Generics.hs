module GenericsPuzzles where

-- These puzzles require the uniplate package that can be installed
-- with the command:
--
--     cabal install uniplate

import Data.Generics.Uniplate.Data

-- Find all values that match some filter

-- Replace all values that match some filter

